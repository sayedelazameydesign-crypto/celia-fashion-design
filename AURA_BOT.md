# 🤖 Aura-Bot - دليل الاستخدام

## نظرة عامة

**Aura-Bot** هو الوكيل الذكي لأزياء جيل Z في منصة Celia Fashion Design (celia_fashion_design). مصمم للتفاعل بأسلوب GEN-Z شبابي مع مصطلحات مثل "Level Up" 🎮، "Epic Loot" 💎، "Glow Up" ✨.

## الميزات الرئيسية

### 1. نصائح موضة مخصصة ✨
- يتفاعل بأسلوب GEN-Z شبابي ومرح
- يقدم ترشيحات بناءً على:
  - العمر (age_range)
  - المقاس (size)
  - الألوان المفضلة (favorite_colors)
  - الميزانية (budget)
- يعرض 2-3 منتجات معتمدة فقط (approved products)
- يظهر السعر النهائي (بعد إضافة 15% هامش ربح)

### 2. البحث البصري 🔍
- رفع صورة لأي ملابس
- تحليل الصورة باستخدام Gemini Vision API
- إرجاع أفضل 3 منتجات مشابهة
- عرض معلومات كاملة: الاسم، الصورة، السعر، رابط الشراء

### 3. دعم اللغات 🌍
- دعم تلقائي للعربية والإنجليزية
- يتحدث بنفس لغة المستخدم تلقائياً
- يمكن تحديد اللغة يدوياً عبر معامل `lang`

## API Endpoints

### POST `/api/ai/advice`

الحصول على نصائح موضة مخصصة.

**Request Body:**
```json
{
  "query": "أريد ستايل جيمينج",
  "age_range": "16-20",
  "size": "M",
  "favorite_colors": "أسود، ذهبي",
  "budget": 500,
  "lang": "ar"
}
```

**Response:**
```json
{
  "advice": "🔥 Level Up! وجدنا لك ستايل Epic...",
  "products": [
    {
      "id": "uuid",
      "name": "منتج",
      "category": "شباب GEN-Z",
      "final_price": 575,
      "original_price": 500,
      "image": "url",
      "description": "...",
      "sizes": ["M", "L"],
      "colors": ["أسود"],
      "tags": ["gaming"],
      "rarity": "Epic",
      "style_points": 1500
    }
  ],
  "lang": "ar"
}
```

### POST `/api/ai/visual-search`

البحث البصري باستخدام الصورة.

**Request Body:**
```json
{
  "image": "base64_encoded_image",
  "lang": "ar"
}
```

**Response:**
```json
{
  "keywords": ["أسود", "جينز", "ستايل شبابي"],
  "products": [
    {
      "id": "uuid",
      "name": "منتج مشابه",
      "final_price": 460,
      "image": "url",
      "ctaUrl": "/product/uuid"
    }
  ],
  "lang": "ar",
  "message": "🔥 وجدنا منتجات مشابهة!"
}
```

## استخدام Aura-Bot في الواجهة الأمامية

### مثال بسيط

```typescript
import { aiAPI } from '../lib/api';

// نصائح موضة
const response = await aiAPI.getAdvice({
  query: 'أريد ستايل جيمينج',
  age_range: '16-20',
  budget: 500,
  lang: 'ar'
});

console.log(response.advice); // نصيحة Aura-Bot
console.log(response.products); // المنتجات الموصى بها
```

### مثال البحث البصري

```typescript
// تحويل الصورة إلى base64
const reader = new FileReader();
reader.onloadend = async () => {
  const base64 = reader.result.split(',')[1];
  
  const response = await aiAPI.visualSearch(base64, 'ar');
  console.log(response.products); // أفضل 3 منتجات مشابهة
};
reader.readAsDataURL(imageFile);
```

## معايير Aura-Bot

### التصنيفات المدعومة
- **أطفال نيون** (Neon Kids)
- **شباب GEN-Z** (GEN-Z Youth)
- **إكسسوارات ملكية** (Royal Accessories)
- **أحذية ذكية** (Smart Kicks)

### مصطلحات GEN-Z
- Level Up 🎮
- Epic Loot 💎
- Glow Up ✨
- Style Buff ⚡
- Vibes 🔥
- Hero Mode 👑

### Emojis المستخدمة
- 🔥 - الحماس/النار
- ✨ - التألق/السطوع
- 👟 - الأحذية
- 👑 - الإكسسوارات الملكية/الفخمة
- 🎮 - الجيمينج/التقنية
- ⚡ - الطاقة/السرعة

## القواعد المهمة

1. **منتجات معتمدة فقط**: Aura-Bot لا يعرض أبداً منتجات غير معتمدة (pending/rejected)

2. **هامش الربح**: جميع الأسعار المعروضة تتضمن 15% هامش ربح (final_price = original_price × 1.15)

3. **الحد الأقصى**: يعرض 2-3 منتجات فقط في كل استجابة

4. **البدائل**: إذا لم يوجد منتج مناسب، يقترح بدائل من نفس التصنيف

5. **اللغة**: يتحدث بنفس لغة المستخدم تلقائياً (يمكن تحديدها يدوياً)

## التكامل مع StylistAI Component

المكون `StylistAI.tsx` يدعم:
- ✅ إدخال نصي (query)
- ✅ البحث البصري (صورة)
- ✅ فلاتر اختيارية (عمر، مقاس، ألوان، ميزانية)
- ✅ عرض المنتجات الموصى بها
- ✅ روابط شراء مباشرة

## أمثلة استفسارات

### بالعربية
- "نسقي لي إطلالة للجيمينج 🎮"
- "أريد ستايل مدرسة نيون 🎒"
- "إكسسوارات ترفع ليفل الأناقة ⚡"
- "ملابس للعمر 16-20 بميزانية 500 ر.س"

### بالإنجليزية
- "Style me for gaming session"
- "I need neon school outfit"
- "Accessories that level up my style"
- "Clothes for age 16-20, budget 500 EGP"

---

**تم البناء بـ ❤️ للموضة المستقبلية**

