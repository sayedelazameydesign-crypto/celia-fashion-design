# 🚀 النشر على Replit - Replit Deployment Guide

لقد قمت بإعداد الملفات اللازمة لتشغيل المشروع على Replit بسهولة.

## 📋 الخطوات المطلوبة:

1. **استيراد المشروع**:
   - افتح [Replit.com](https://replit.com)
   - اضغط على **"Create Repl"** ثم **"Import from GitHub"**
   - الصق رابط المستودع: `https://github.com/sayedelazameydesign-crypto/celia-fashion-design`

2. **إعداد متغيرات البيئة (Secrets)**:
   - في Replit، اذهب إلى قائمة **"Secrets"** (أيقونة القفل)
   - أضف جميع القيم الموجودة في ملف `.env.example` كـ Secrets:
     - `GEMINI_API_KEY`
     - `SUPABASE_URL`
     - `SUPABASE_ANON_KEY`
     - `SUPABASE_SERVICE_ROLE_KEY`
     - `VITE_API_URL` (استخدم رابط الـ API الخاص بـ Replit عند تشغيله)

3. **التشغيل**:
   - اضغط على زر **"Run"** الكبير في الأعلى.
   - سيقوم Replit بتثبيت المكتبات تلقائياً وتشغيل كل من الخادم (Port 5000) والواجهة (Port 3000).

## 🛠 التعديلات التي تمت:
- إضافة ملف `.replit` لإدارة التشغيل التلقائي.
- إضافة ملف `replit.nix` لتحديد بيئة Node.js.
- استخدام `concurrently` لتشغيل الـ Frontend والـ Backend معاً.

---
**جاهز للنشر على Replit! 🚀**